/**
 * Returns a list of allowed command keys.
 * @returns {string[]}
 */
export default function() {
    return ["name", "aliases", "type", "code", "additionalData", "async", "minify", "transpiled", "path", "reloadable"]
}